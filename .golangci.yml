version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - errorlint
    - exhaustive
    - nilerr
    - nolintlint

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
